#pragma once

#include "libX.hpp"

class Utils
{
    public:
            static String  getCmd(String entry);
    private:
            Utils();
            //Utils(const Utils &ref);
            //~Utils();
            //Utils &operator=(const Utils &ref);
};